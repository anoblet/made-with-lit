import { css } from "lit-element";

export default css`
    .card {
        border: 1px solid #000;
        border-radius: 0.25rem;
        padding: 1.5rem;
    }

    .grid {
        display: grid;
        grid-gap: 1.5rem;
    }
`;
